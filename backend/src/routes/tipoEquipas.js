/* 
const express = require('express');
const router = express.Router();

const TipoEquipaController = require("../controllers/TipoEquipaController");

router.get("/", TipoEquipaController.tipoEquipa_list); 

module.exports = router; */